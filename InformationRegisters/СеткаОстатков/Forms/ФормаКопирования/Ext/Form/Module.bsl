
&НаКлиенте
Процедура Копировать(Команда)
	
	Если ЗначениеЗаполнено(НоменклатурнаяГруппа) И ЗначениеЗаполнено(Подразделение) Тогда
		
		Пар = Новый Структура;
		Пар.Вставить("ИсточникПодразделение",Подразделение);
		Пар.Вставить("ИсточникНомГруппа",НоменклатурнаяГруппа);
		
		Оповестить("Копирование",Пар);
	    Закрыть();
		
	Иначе
		Сообщить("Выберите подразделение и номенклатурную группу для заполнения");
	КонецЕсли;
	
КонецПроцедуры
