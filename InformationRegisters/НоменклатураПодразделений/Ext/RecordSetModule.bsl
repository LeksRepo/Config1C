
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для каждого Запись Из ЭтотОбъект Цикл
		
		Если Запись.Подразделение.ВидПодразделения <> Перечисления.ВидыПодразделений.Производство Тогда
			ТекстОшибки = "Установка доступности номенклатуры возможна только для производственных подразделений";
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки);
			Отказ = Истина;
		КонецЕсли;
		
		Если НЕ Запись.Номенклатура.Базовый Тогда
			ТекстОшибки = "Установка доступности возможна только для базовой номенклатуры";
			ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстОшибки);
			Отказ = Истина;
		КонецЕсли;
		
		Если Запись.ПодЗаказ И НЕ Запись.Номенклатура.НоменклатурнаяГруппа.ПринадлежитЭлементу(Справочники.НоменклатурныеГруппы.Кромка) Тогда
			Запись.ПроцентОтхода = 0;
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры
