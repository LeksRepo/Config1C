
&НаСервере
Процедура ЗаписатьНаСервере()
	
	Об = Наряд.ПолучитьОбъект();
	Об.ЗапретРедактирования = ЗапретРедактирования; 
	Об.Записать();
	
КонецПроцедуры

&НаКлиенте
Процедура Записать(Команда)
	ЗаписатьНаСервере();
	ЭтаФорма.Закрыть();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Наряд = Параметры.Наряд;
	ЗапретРедактирования = Наряд.ЗапретРедактирования;
	
КонецПроцедуры
