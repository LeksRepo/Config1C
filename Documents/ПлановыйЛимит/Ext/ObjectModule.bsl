
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	УдалитьИзРегистра();
	
	
	//Набор.
	//Набор = РегистрыНакопления.ЦеховойЛимит.СоздатьНаборЗаписей();
	//Набор.Отбор.Регистратор.Установить(ЭтотОбъект.Ссылка);
	//Набор.Прочитать();
	
	Для Каждого День Из ТаблицаДней Цикл
		
		Запись = Движения.ЦеховойЛимит.Добавить();
		Запись.Период = НачалоДня(День.Дата);
	    Запись.КоличествоКоробов = День.КоличествоКоробов;
		Запись.СтоимостьУслуг = День.Норматив;
	    Запись.Подразделение = Подразделение;
		//Запись.Запи
	 	
	КонецЦикла;
	
КонецПроцедуры

Процедура УдалитьИзРегистра()
	
	Набор = РегистрыНакопления.ЦеховойЛимит.СоздатьНаборЗаписей();
	Набор.Отбор.Регистратор.Установить(ЭтотОбъект.Ссылка);
	Набор.Прочитать();
	Набор.Очистить();
	Набор.Записать();
	
КонецПроцедуры

