
Процедура ПередЗаписью(Отказ)
	
	Если НЕ ЗначениеЗаполнено(Ссылка) Тогда
		// новый элемент
		Отказ = Истина;
	Иначе
		Если Родитель <> Ссылка.Родитель Тогда
		// Запрещено перемещать по папкам
			Отказ = Истина;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры